import { css } from 'styled-components';

export const font12 = (weight: number) => css`
  font-size: 12px;
  line-height: 1.6666666667;
  font-weight: ${weight};
`;

export const font14 = (weight: number) => css`
  font-size: 14px;
  line-height: 1.2;
  font-weight: ${weight};
`;

export const font15 = (weight: number) => css`
  font-size: 15px;
  line-height: 1.2;
  font-weight: ${weight};
`;

export const font16 = (weight: number) => css`
  font-size: 16px;
  line-height: 1.1875;
  font-weight: ${weight};
`;
