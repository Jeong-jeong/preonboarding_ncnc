import { css } from 'styled-components';

export const font12 = (weight: number) => css`
  font-size: 12px;
  line-height: 1.6666666667;
  font-weight: ${weight};
`;

export const font14 = (weight: number) => css`
  font-size: 14px;
  line-height: 1.4285714286;
  font-weight: ${weight};
`;

export const font16 = (weight: number) => css`
  font-size: 16px;
  line-height: 1.5;
  font-weight: ${weight};
`;

export const font20 = (weight: number) => css`
  font-size: 20px;
  line-height: 1.6;
  font-weight: ${weight};
`;
